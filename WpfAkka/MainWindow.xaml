<Window x:Class="WpfAkka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfAkka.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- DataTemplate for EventMessageViewModel -->
        <DataTemplate x:Key="EventMessageTemplate" DataType="d:">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Path=Message}" Padding="5"/>
                <ToggleButton Command="{Binding TogglePopupCommand}" Content="&#xE712;" FontSize="16" FontFamily="Segoe MDL2 Assets" />
                <Popup IsOpen="{Binding IsPopupOpen}" Placement="Right" StaysOpen="False">
                    <Border BorderBrush="Black" BorderThickness="1" Background="White">
                        <TextBlock Text="{Binding Path=Payload}" Padding="5"/>
                    </Border>
                </Popup>
            </StackPanel>
        </DataTemplate>

        <!-- DataTemplate for PluginViewModel -->
        <DataTemplate x:Key="PluginTemplate">
            <GroupBox Header="{Binding Name}">
                
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                    <StackPanel Width="100">
                        <CheckBox x:Name="isAvailable" Content="Is Available" IsChecked="{Binding IsAvailable}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" >

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ProcessedMessagesCount, StringFormat='Processed ({0}):'}" />
                            <ListBox ItemsSource="{Binding ProcessedMessages}" ItemTemplate="{StaticResource EventMessageTemplate}">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                            </ListBox>
                        </StackPanel>
                    </StackPanel>
                    </Grid>
                </GroupBox>
                

        </DataTemplate>
    </Window.Resources>

    <Grid d:DataContext="{d:DesignInstance Type=vm:DesignMainWindowViewModel, IsDesignTimeCreatable=True}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" BorderBrush="Black" BorderThickness="1" Margin="5" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Plugin" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBlock Grid.Column="1" Text="Scenario" VerticalAlignment="Center" Margin="5"/>

                <Grid Grid.Row="1" Width="90">
                    <ComboBox ItemsSource="{Binding PluginSelections}"
                      DisplayMemberPath="Value" 
                      SelectedValuePath="Key" 
                      SelectedItem="{Binding SelectedPlugin}" />
                    <TextBlock Text="{Binding SelectedPlugin}" Margin="0,40,0,0" />
                </Grid>
                

                <Grid Grid.Row="1" Grid.Column="1" >
                    <ComboBox ItemsSource="{Binding Scenarios}"
                      DisplayMemberPath="Scenario"
                      IsReadOnly="True"
                      SelectedItem="{Binding SelectedScenario}"/>
                    <TextBlock Text="{Binding SelectedScenario}" Margin="0,40,0,0" />
                </Grid>
                
                <Button Grid.Row="1" Grid.Column="2" Content="Run Scenario" Command="{Binding RunScenarioCommand}" Margin="5"/>
            </Grid>
            
        </Border>
        
        <ListBox Grid.Row="1" ItemsSource="{Binding Plugins}" ItemTemplate="{StaticResource PluginTemplate}" Margin="5,5,5,5" Grid.ColumnSpan="2" HorizontalContentAlignment="Stretch"/>
    </Grid>

</Window>
